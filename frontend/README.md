# ⚠️ Orphan artifact directory

This lowercase `frontend/` tree is **not** the product Console. The supported SPA lives in **`FRONTEND/`** (uppercase).

Do not build, deploy, or document this directory. It may contain stale `node_modules` from an old checkout and is ignored by supported compose files.
