# Development environment variables for Vite
# These are embedded at build time, not runtime

# API base URL - empty means use relative URLs (Vite dev server handles proxying)
VITE_API_URL=

# App name displayed in UI
VITE_APP_NAME=Fraud Detector Dev

# Enable debug logging (true in development)
VITE_ENABLE_DEBUG=true
