# Production environment variables for Vite
# These are embedded at build time, not runtime

# API base URL - empty means use relative URLs (nginx handles proxying)
VITE_API_URL=

# App name displayed in UI
VITE_APP_NAME=Fraud Detector

# Enable debug logging (false in production)
VITE_ENABLE_DEBUG=false
