package com.posgateway.aml.config;

import org.springframework.context.annotation.Configuration;

/**
 * Tracing configuration placeholder — OpenTelemetry SDK not on classpath in test/build env.
 * Add io.opentelemetry dependencies to pom.xml to enable distributed tracing.
 */
@Configuration
public class TracingConfig {
}
